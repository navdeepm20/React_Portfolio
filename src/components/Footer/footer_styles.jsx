import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  footer: {
    background: "#fff",
    borderRadius: "10px",
    minHeight: "70px",
    padding: "15px",
  },
  footer_name: {
    fontSize: "15px",
    fontWeight: "500",
  },
});
