import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  footer: {
    background: "#1e1e1e",
    borderRadius: "10px",
    minHeight: "70px",
    padding: "15px",
    color: "#fff",
    marginBottom: "30px",
  },
  footer_name: {
    fontSize: "15px",
    fontWeight: "500",
  },
});
